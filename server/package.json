{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.41",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "start:dev": "nodemon",
    "build": "rm -rf ./build && tsc",
    "start": "yarn run build && node build/index.js"
  },
  "dependencies": {
    "mongodb": "^3.6.6"
  }
}
